# Orchestration
dagster>=1.9.0
dagster-webserver>=1.9.0

# Core data
numpy>=1.26.0
pandas>=2.0.3
scipy>=1.11.0

# Excel
openpyxl>=3.1.5

# DB
sqlalchemy>=2.0.0
pyodbc>=5.2.0

# Plotting
matplotlib>=3.8.0
plotly>=5.18.0

# Sandbox
streamlit>=1.35.0

# Finance
QuantLib>=1.33

# Jupyter / dev
ipykernel>=6.29.0
ipython>=8.12.0
nbconvert>=7.0

# Windows
pywin32>=311
colorama>=0.4.6

# Parallel computing (dask.distributed backend in bank_reprice_at_weights.py
# is validated correct -- see that module's docstring; ProcessPoolExecutor
# remains the default since it's faster for this job size, Dask backend is
# available via backend="dask" for future cluster scale-out)
dask[distributed]>=2026.7.1
